# 강사 인증·권한 설계 (#1 — 2차 Security, Firebase Auth + 화이트리스트)

**작성**: 2026-06-21 · **상태**: 설계(미구현) · **선행**: `SECURITY_RULES_PLAN.md`(1차 임시봉인)

> 1차(deny-by-default + 공유 DB Secret)는 **임시벽**. 이 문서의 2차가 완성되면
> 사용자 단말의 만능키(시크릿)를 회수하고 개인 로그인으로 교체한다.

---

## 1. 목표 (왜 하나)
- **퇴사자 차단**: 떠난 강사/관리자의 접근을 즉시 끊는다(계정 비활성·명단 삭제).
- **캠퍼스 격리**: A캠퍼스 강사가 B·C 데이터에 접근 못 하게 **룰로 강제**.
- **만능키 회수**: 사용자 단말에서 공유 시크릿 제거 → 단말 분실/유출=전체노출 위험 해소.
- **사용성**: 일반 강사는 가능한 무마찰(개인 기기 1회 인증 후 자동 유지).

## 2. 핵심 모델 — 개인 신원 + DB 화이트리스트(ACL)
- 모든 사용자는 **개인 신원으로 로그인**(Firebase Authentication).
- 권한은 DB의 **화이트리스트 노드(`acl/`)** 가 결정 — "이 신원 = 어느 캠퍼스·어느 역할".
- 룰은 `auth`(로그인 신원) + `acl/`(명단)만 보고 허용/차단. **앱 클라이언트 체크는 보조일 뿐, 차단은 항상 룰(서버)에서.**
- 커스텀 클레임(Admin SDK 서버 필요) 대신 **DB ACL을 룰이 직접 읽는** 방식 → 별도 백엔드 불필요, 관리자가 ACL만 관리.

```
acl/
  {uid}: { campus: "gangnam", role: "instructor"|"admin"|"super",
           instructorId: "홍길동", active: true }
    · 키 = Firebase Auth uid (불변·고유). 이메일 키는 회피(이메일 변경·'.'치환 취약).
      관리자 도구가 계정 생성 시 uid를 즉시 받으므로 uid 매핑이 자연스러움.
    · active:false 또는 노드 삭제 = 즉시 차단(퇴사). 토큰이 아직 유효해도 룰이
      매 요청 acl.active 를 확인하므로 즉시 효력(개인 계정의 강점).
    · instructorId = 기존 config/instructors 키(한글 이름) — 로그인 신원↔기존 데이터 연결(§12).
      권한 판단엔 campus/role/active만 사용.
    · role: instructor / admin(캠퍼스) / super(전 캠퍼스 — §11 결정)
```

### 2.1 개인 DB 설정 제거 (로그인의 부수 이득)
로그인이 들어오면 사용자가 직접 넣던 DB 설정 3종이 모두 불필요해진다:
- **DB URL**: 앱 빌드에 **내장**(고정). Firebase DB URL은 비밀이 아니다 — 보안은 룰+auth가 담당(웹 Firebase 설정은 원래 공개되는 값).
- **경로/캠퍼스**: 로그인 후 `acl/{me}/campus` 를 읽어 **자동 판별** — 사용자가 고르지 않음.
- **DB Secret**: **제거** — 로그인 토큰이 대체. 단말에서 만능키가 사라져 유출면 동시 해소.

→ 강사 온보딩: "앱 실행 → 로그인"이 전부. 설정(`drw_db_url/path/secret`) 입력란은 **관리자 전용으로 축소 또는 제거**.
→ **단일 Firebase 프로젝트 확정**: 캠퍼스별로 프로젝트(창고)를 쪼개면 "어느 DB에 붙어 acl을 읽나"가 닭-달걀이 됨. 단일 프로젝트 + 캠퍼스는 acl/경로로 구분(§9 #3 해소).

## 3. 인증 방식 — **이름(한글)+비밀번호** (합성 이메일 매핑)
사용자는 **한글 이름 + 비밀번호**로 로그인한다. 단 Firebase Auth(이메일/비번 provider)는
**아이디가 이메일 형식 필수** → 한글 이름을 그대로 못 씀. 그래서 앱이 내부에서
**이름 → 합성 이메일** 로 변환해 `signInWithPassword` 호출. **사용자는 이메일을 보지도 입력하지도 않음.**
```
입력: 이름="홍길동", 비번="****"
내부: signInWithPassword( synthEmail("홍길동"), "****" )
       synthEmail(name) = slug(name) + "@" + campus + ".drw.local"   // ASCII 인코딩, 도메인 미발송
```
- 합성 이메일은 **결정적**(같은 이름→같은 이메일)이라 로그인 재현됨. 한글은 ASCII로 인코딩
  (punycode/percent 등) — 사용자엔 안 보임.
- 기존 신원이 `config/instructors/{이름}` 이라 **이름 로그인이 기존 모델과 정확히 일치** → 이관 매끄러움(§12).
- (매직링크·익명 등은 이름 로그인과 무관하게 탈락 — 이름+비번으로 확정.)

### 3.0 이름 유일성 (동명이인)
합성 이메일은 유일해야 함. 정책 택1:
- **(권장) 캠퍼스 스코프**: `slug(name)@{campus}.drw.local` → 캠퍼스 내 유일하면 됨.
  로그인 폼에 **캠퍼스 선택 + 이름 + 비번**(캠퍼스 드롭다운 1회).
- **전역 유일 이름**: 운영자가 이름 충돌 시 접미(홍길동2) 부여 → 로그인은 이름+비번만(캠퍼스 선택 불요).
- 동명이인은 기존 `config/instructors` 키에서도 충돌 위험이었음 — 이관 시 점검.

### 3.1 계정·비밀번호 관리 (관리자 생성형)
- **비밀번호는 Firebase Auth가 해시(scrypt)로 보관 — 관리자·앱 누구도 실제 비번을 보지 못한다.**
- **발급**: 관리자가 ClassManager(또는 웹 관리자)에서 계정 생성(이름 입력 → 합성 이메일 자동) +
  **임시 비밀번호** 지정 → 강사에게 **이름+임시비번** 개별 전달(비공개 채널).
- **첫 로그인 시 비번 변경 강제** → 이후 본인만 앎.
- **분실 처리 — 관리자 리셋 확정**: 합성 이메일(`@…drw.local`)은 실제 메일이 안 가므로
  셀프 메일 재설정 불가. **관리자가 도구에서 비번 리셋 후 재전달**. (강사 실이메일 수집 불필요.)
- **퇴사**: `acl/{me}.active=false`(즉시 차단) + 필요 시 Auth 계정 비활성. 비번 회전 불필요(개인 계정).
- 계정 생성/삭제·비번 리셋 UI = 관리자 도구. 실제 비번 값은 어디에도 평문 저장 안 됨(해시).

## 4. 권한 룰 스케치 (경로별·역할별)
**캠퍼스 분리 = 경로 분리 확정**(`drw2_{campus}`). 단일+필드보다 룰이 훨씬 단순 —
서브트리마다 campus가 상수라 레코드별 검사·교차쓰기 방지가 필요 없다. `acl/`는
**루트(캠퍼스 경로 밖)** 에 둔다(로그인 시 campus 모른 채 먼저 읽어야 하므로).

```
// 헬퍼: 현재 사용자의 acl 엔트리 (uid 키)
me()        = root.child('acl').child(auth.uid)
active      = auth != null && me().child('active').val() == true
inCampus(C) = active && (me().child('campus').val()==C || me().child('role').val()=='super')
role        = me().child('role').val()

acl/
  ".read":  "role=='admin' || role=='super'"          // 명단 조회는 관리자/본사만
  acl/$uid ".read": "auth.uid==$uid"                   // 본인 엔트리는 본인도 읽기(campus 파악용)
  acl/$uid ".write":
    "role=='super'                                     // super: 전체(admin 임명 포함)
     || (role=='admin'
         && newData.child('campus').val()==me().child('campus').val()  // 자기 캠퍼스만
         && newData.child('role').val()=='instructor'                  // instructor만 생성/수정
         && (!data.exists() || data.child('role').val()=='instructor'))// admin/super 대상 변경 금지
    "
            // → admin은 자기 캠퍼스 instructor만 관리. 자신·타인 admin/super 승격 불가(권한상승 차단).

drw2_{campus}/                                          // 예: drw2_gangnam
  ".read":  "inCampus('gangnam')"
  students,classes 쓰기:        "inCampus('gangnam') && (role=='admin'||role=='super')"
  input,obs,scores,session 쓰기: "inCampus('gangnam')"   // 강사·관리자 공통(수업 운영)
  history 쓰기:                  "inCampus('gangnam')"   // PC 전송 경로
```
- 캠퍼스 격리 = `acl.campus == 경로 campus` 대조로 룰 강제(super는 전 캠퍼스 통과).
- 강사는 명단(students/classes) **읽기만**, 추가·삭제는 관리자(기존 v8.13 권한분리를 룰화).
- 강사 acl.active=false → **토큰 유효해도 즉시 차단**(룰이 매 요청 확인).
- (선택·후속) 캠퍼스 내 "담임 본인 수업만 쓰기"까지 룰로 좁히려면 배정맵 필요 —
  v1은 캠퍼스를 신뢰경계로 두고 캠퍼스 단위까지만 강제, 그 안은 클라 UI 가드 유지.

## 5. 계정·역할 — 전원 개인 계정 (공유 없음, 확정)
**공유 계정 폐기.** 강사·관리자 모두 **개인 계정**. 역할은 acl의 `role` 꼬리표로만 구분.
- 퇴사(강사·관리자 무관) = `acl.active=false` → **즉시 차단**(토큰 살아 있어도 룰이 매 요청 확인).
- "누가 했는지" 기록 보존(개인 신원). 공유 비번 유출·회전 골칫거리 없음.

### 역할 3단
| role | 범위 | 할 수 있는 것 | 관리 주체 |
|------|------|--------------|----------|
| **instructor** | 자기 캠퍼스 | 입력·관찰·성적·전송, 명단 읽기 | 캠퍼스 admin |
| **admin** | 자기 캠퍼스 | + 명단 CRUD, **자기 캠퍼스 instructor 계정 관리**, CM·Analyzer | super |
| **super** | 전 캠퍼스 | + **admin 임명/회수**, 통합 분석, 캠퍼스 신설, 부트스트랩 | 콘솔(owner) |

- **admin은 instructor만 관리** — 다른 admin·super를 만들거나 자신을 승격 못 함(권한상승 차단, §4 룰).
- **super는 admin을 관리** — "관리자를 관리하는 사람". 1~2명, 거의 안 바뀜 → **Firebase 콘솔로 수동 관리**(전용 UI 불필요, 무한회귀 방지).

### super = 시스템 관리자 1인 (운영자) — 확정
현실: 사업 오너가 시스템 관리를 할 리 없음 → super는 **시스템 관리자 1인(운영자)** 이 맡는다.
이 사람이 Firebase **콘솔 owner**(인프라: 룰·스키마·잠금복구)와 **in-app super**(데이터·admin
관리)를 **겸함**. 두 역할을 한 사람이 쓰는 구조.
- admin 임명/회수(=acl 쓰기)는 **콘솔에서 직접 acl 편집**으로도 가능 → 전용 super 화면 없이 운영.
  in-app super 계정은 통합 분석 등 데이터가 필요할 때만(선택).
- ⚠️ **이 1인 계정이 전체 PII 마스터 접근** = 최고 위험 집중점. 분리를 안 하는 대신 **그 계정을
  강하게 보호**가 실질 방어:
  - Firebase 콘솔 계정(구글)에 **2단계 인증(MFA) 필수**, 강·고유 비번, 타 서비스 비번 재사용 금지.
  - 가능하면 운영 전용 구글계정 분리(개인 메일과 분리).
- super 가 굳이 in-app 데이터계정으로 상주할 필요는 적음 — **평소엔 콘솔 owner로만, 분석 등 필요 시
  super 로그인**. 상주 최소화가 노출면을 줄인다.

## 6. 클라이언트별 적용 (작업 규모순)
| 클라 | 사용자 | 적용 |
|------|--------|------|
| **웹앱(DRW)** | 강사 전원 + 관리자 | 🔴 본격 — 로그인 화면 + 세션 + 시크릿 제거. 핵심 |
| ClassManager | 관리자만 | 관리자 로그인 게이트 + acl 관리 UI(강사 계정 발급/삭제) |
| Analyzer | 관리자만 | 관리자 로그인 게이트(read-only) |
| PC앱(DRW) | 데스크/관리 직원(가정) | 관리자 로그인 또는 통제 PC 시크릿 유지. 강사 각자 운영이면 강사 로그인 추가 |

- **강사 계정 발급/삭제(=acl 관리)는 관리자 도구(CM 또는 웹 관리자)에서.** 이메일로 등록 → 강사 첫 로그인 시 매칭.

## 6.1 교차-클라 동일 계정 (웹 ↔ PC)
**같은 이메일+비번 계정 하나로 웹·PC 모두 로그인.** acl/룰은 클라 종류를 구분하지 않음 —
한 사람=한 신원. 시나리오는 동일하되 구현만 다르다:
- **웹앱**: Firebase JS SDK가 토큰 발급·1시간 갱신·세션 유지를 자동 처리.
- **PC앱(Python)**: REST로 직접 처리 — 공식 클라이언트 Auth SDK 없음. 필요 배관:
  1. 로그인 `POST identitytoolkit…/accounts:signInWithPassword?key={웹APIKey}` → `idToken`(1h)+`refreshToken`.
  2. RTDB REST 호출의 `?auth=` 를 **시크릿 → idToken** 으로 교체(`firebase.py _fb_url` 소폭 수정).
  3. idToken 만료 전 `POST securetoken…/token` (refresh_token)로 자동 재발급.
  4. **refreshToken 은 DPAPI 암호화 저장** — 기존 `secret_codec` 재활용(매번 재로그인 방지).
  - 필요한 키 = **Firebase 웹 API Key**(공개값, 비밀 아님).
- **퇴사 자동 반영**: 계정 비활성/비번 변경 → refreshToken 무효화 → 웹·PC 모두 다음 갱신 시 차단·재로그인 강제.

## 7. 만능키 → 로그인 무중단 전환 절차
1. Firebase Authentication 활성화(이메일/비번), 관리계정·강사계정 사전 발급, `acl/` 시드.
2. 룰을 **"시크릿 OR (auth+acl)"** 병행 허용으로 1차 배포 → 기존 시크릿 클라 안 끊김.
3. 클라이언트 순차 업데이트(로그인 탑재) + 사용자 로그인 전환 확인.
4. 전원 전환 확인 후 룰에서 **시크릿 우회 제거**(auth+acl만 허용).
5. Firebase 콘솔에서 **레거시 DB Secret 폐기**(회전) → 만능키 무효화. 백업 스크립트는 서비스계정/통제PC로 이관.

## 8. 단계별 작업 순서 (권장)
1. (선행) 1차 임시봉인 `deploy-rules.ps1` — 공개 차단하고 파일럿 출시.
2. 경로/캠퍼스 네이밍 확정(`drw2_{campus}`) + acl 스키마 확정.
3. Firebase Auth 활성화 + 룰 병행 버전 작성.
4. 웹앱 로그인 구현(최우선) → CM 관리자 게이트+acl관리 UI → Analyzer → PC.
5. 사용자 전환 → 시크릿 우회 제거 → 레거시 시크릿 폐기.

## 10. 설계 보강 — 엣지·보안 디테일 (검토 발굴)
- **G1 가입 선점 차단(중요)**: Firebase 이메일/비번은 클라가 임의 가입 가능 →
  공격자가 미래에 acl 등록될 이메일을 **미리 선점**하면 그 계정을 장악. **방어**:
  ① 관리자 도구가 **계정을 직접 생성**(uid 확보)한 뒤에만 acl 등록 — 아직 없는 이메일을
  acl에 넣지 않는다. ② 가능하면 셀프 가입 비활성. ③ acl이 최종 게이트라 acl 없는
  가입자는 어차피 접근 0(이메일 유니크 제약상 기존 계정 이메일 재가입 불가).
- **G3 acl 위치/읽기**: acl은 **루트**(캠퍼스 경로 밖). 본인 엔트리는 본인 읽기 허용
  (로그인 직후 campus 파악), 전체 명단 읽기는 관리자/super만(프라이버시).
- **G5 첫 관리자 부트스트랩**: acl 쓰려면 관리자, 관리자 되려면 acl — 닭/달걀.
  **전환 창에서 콘솔(또는 레거시 시크릿)로 최초 super/admin acl 1건 수동 시드.**
- **G6 브레이크글래스(잠금복구)**: 전환 중(시크릿 병행 단계)엔 레거시 시크릿이 비상문.
  시크릿 폐기 후엔 **Firebase 콘솔 룰 직접수정**이 유일 복구 → 프로젝트 owner 1인 상시 확보.
- **G7 오프라인 한계**: 로그인·토큰갱신은 네트워크 필요 → 무네트워크 시 접근 불가
  (acl 조회 불가). 동기화도 어차피 네트워크 필요하므로 수용. PC 진도 캐시는 읽기용으로만 유지.
- **G10 규모/비용**: Auth 사용자 ~70명(20×3+관리자) → 무료 한도(월 수만 MAU) 무관.
  룰의 acl 조회 비용 무시 가능. **비용·성능 우려 없음.**
- **G11 캠퍼스 내 범위**: v1은 캠퍼스가 신뢰경계 — 캠퍼스 안 강사 간 입력/관찰 상호접근은
  룰로 안 막고 클라 UI(담임 가드)만. 담임별 쓰기 제한이 필요해지면 후속(배정맵 룰).

## 11. 결정·남은 확인
- [x] **계정 방식**: 전원 개인 계정(공유 폐기). 관리자도 개인+role=admin → acl.active 즉시차단(§5)
- [x] **역할 3단**: instructor / admin(캠퍼스) / super(전 캠퍼스). admin은 instructor만 관리,
  super가 admin 관리. 권한상승 차단 룰(§4)
- [x] **super 주체**: **시스템 관리자 1인(운영자)** 확정. 콘솔 owner + in-app super 겸함(§5)
- [x] **계정 보호**: super/콘솔 계정 MFA·강비번 필수(마스터 접근 집중점, §5)
- [ ] **Analyzer 범위**: admin=자기 캠퍼스 / super=전 캠퍼스 통합 (super 데이터접근 쓸지 결정)

## 9. 결정 현황
- [x] **인증 방식**: **한글 이름+비밀번호** 확정 — 앱이 이름→합성 이메일 변환(§3)
- [x] **캠퍼스 분리**: 단일 프로젝트(§2.1) + **경로 분리 `drw2_{campus}` 확정**(룰 단순, §4)
- [x] **ACL 키**: uid 기반 확정(이메일 키 취약점 회피, §2)
- [x] **계정 생성**: 관리자 생성형 + 임시비번 개별 전달 + 첫로그인 변경강제(§3.1)
- [x] **강사 기기**: 이름+비번이 개인·공용 양쪽 지원 → 설계 영향 없음. 개인기기=세션유지,
      공용 태블릿=세션 짧게+로그아웃 버튼(운영 정책만, 구현 동일)
- [x] **계정 방식**: 전원 개인 계정(공유 폐기), 역할 3단(§5·§11)
- [x] **비번 분실 처리**: 관리자 리셋 확정(합성 이메일이라 셀프 메일 불가, §3.1)
- [x] **super 주체**: 시스템 관리자 1인(운영자) — 콘솔 owner 겸함 (§5)
- [~] **PC앱 운영 주체**: 기본 가정 = **데스크/관리 직원 소수 운영** → PC는 관리자 로그인 또는
      통제 PC 시크릿 유지(§6). 강사 각자 PC 전송이면 PC에도 강사 로그인 필요 — **운영 확인 후 확정**
- [ ] **이름 유일성**: 캠퍼스 스코프(로그인에 캠퍼스 선택) vs 전역 유일 이름(접미) 택1(§3.0)

## 12. CBT 테스터 무중단 이관
**현재 강사 신원** = `config/instructors/{이름}` (이름이 곧 키, 비밀번호 없음). 이 노드에
담당수업(`assignments`)·문구(`presets`)가 들어 있고, `history.instructor`·ai_style 문체분석도
이 이름을 기준으로 동작한다. 즉 신원은 "자기 이름을 입력하면 그 강사가 되는" 비인증 모델.

**핵심: 로그인 계정을 기존 이름에 연결만 하면 데이터 이동이 0이다.**
```
acl/{uid} = { campus, role, instructorId: "홍길동"(기존 이름), active: true }
```
로그인 후 앱은 `acl.instructorId` 를 기존 `instructor_id` 자리에 사용 →
담당수업·문구·과거 이력·문체가 **전부 그대로** 따라온다(레코드 마이그레이션 불필요).

### 새로 수집할 것 (현재 DB에 없음)
- 테스터별 **소속 캠퍼스** — 현재 CBT는 단일 풀이라 없음.
- 테스터별 **임시 비밀번호** — 운영자가 발급.
- (이메일은 **불필요** — 로그인 아이디가 한글 이름이고, 이름은 이미 `config/instructors`에 있음.)

### 이관 작업 순서
1. **명단 추출**: `config/instructors/` 읽어 이름·assignments 목록화(이미 DB에 있음).
2. **캠퍼스 배정 + 임시비번 발급**: 운영자가 테스터별 캠퍼스·임시비번 지정.
3. **계정 일괄 생성**(Admin SDK 스크립트): 이름→합성 이메일로 Auth 계정(+임시비번) +
   `acl/{uid}` 작성(`instructorId`=기존 이름, campus, role).
4. **앱 수정**: 로그인(이름+비번) 후 `instructorId` 를 acl에서 취득 — 기존 "이름 입력 / 로컬 config" 경로 대체.
5. **무중단 컷오버**: 룰을 기존(공개) **OR** 로그인 병행 → 테스터가 로그인해 본인 데이터 확인 →
   전원 전환 후 **로그인 전용**으로 닫고 기존 접근 차단.
6. **자격 전달**: 이름+임시비번 개별 전달, 첫 로그인 시 비번 변경.

→ **결과**: 테스터는 첫 로그인 즉시 본인 담당수업·문구·과거 이력이 그대로 보임. 재설정 0.

### 주의
- **경로 분리(`drw2_{campus}`)는 이관과 동시에 하지 말 것.** 기존 단일 경로를 "캠퍼스 1개"로
  두고 **로그인 이관 먼저**, 경로 분리는 후속 단계(이동 요소 최소화 → 사고 위험↓).
- 이름 중복(동명이인)은 기존에도 키 충돌 위험 — 이관 시 점검(uid는 안전, instructorId만 주의).
